"""goeapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from goeapi.api_helper import APIHelper
from goeapi.models.assets_held_away_info import (
    AssetsHeldAwayInfo,
)
from goeapi.models.fixed_asset import FixedAsset
from goeapi.models.goal_profile import GoalProfile
from goeapi.models.policy_details import PolicyDetails


class UnifiedPortfolioAdviceInputModelV4(object):
    """Implementation of the 'UnifiedPortfolioAdviceInputModelV4' model.

    Attributes:
        is_new_goal_priority (bool): If the investor/end user changes the priority of
            any of the goals within the plan,             in between the
            re-allocation dates, this should be set to ‘true’.             For new
            plans, this needs to be ‘true’.
        is_new_risk_profile (bool): If the risk profile of the investor/end user  has
            changed, this should be set to ‘true’.                      For new
            plans, this needs to be set to ‘true’.
        is_new_investment_tenure (bool): If the investor/end user changes the goal
            investment tenure of any of the goals within the plan,
            in between re-allocation dates, this should be set to ‘true’.For new
            plans, this needs to be ‘true’.
        is_new_goal (bool): If the investor/end user changes the goal amount in
            between the re-allocation dates,              this is set to ‘true’. For
            retirement scenarios, this would be a change in the retirement income
            goal,                  while for a capital accumulation goal, this would
            be a change in the lump sum accumulation target.                      For
            new plans, this needs to be set to ‘true’.
        cashflow_date (str): Cashflow date of the plan - this is typically the date
            (year is ignored) on which infusions/withdrawals would happen for the
            goals.
            Format is "dd-mm-yyyy".
            If the cashflowDate parameter is set to null, the algorithm would
            consider the first reallocation date as the cashflow date.
        get_path (bool): Show portfolio path, wealth path and wealth node path over
            time. If getPath parameter is set to true, the paths would be returned in
            the response payload.
        reallocation_freq (ReallocationFreq2): Describes the frequency of
            re-allocation. If set to 'yearly',             GOE would assume the
            re-allocation to happen once a year.                Response parameters
            such as portfolio path and wealth path would have one value each year.
        initial_investment (float): Defines the lump sum initial welath of the plan.
        current_wealth (float): Current wealth of the plan/combined goal when the GOE
            is being called or executed At the time of initial onboarding,
            currentWealth is null. On subsequent dates, currentWealth would be the
            portfolio account value at the time.
        current_portfolio_id (int): Current portfolio index that the combined plan is
            allocated to; if algo is getting executed for the first time (onboarding
            call), it should be null.        For cases where updated probability is
            needed in between the scheduled re-allocation dates, this parameter
            should be set to the portfolio index the investor is assigned to.
        infusions (List[float]): Net cash flow array for the plan/combined goal.
            Positive in the case of infusions; negative in             case of
            withdrawals; zero in case of no cashflows; frequency depends on
            ‘infusionType’.            For subsequent calls, the infusions array
            needs to be adjusted based on the time that has elapsed.
        risk_profile (RiskProfile6): Defines the user’s risk profile. Note that Risk
            Profiles are mapped to portfolio access. For example, a Conservative
            investor has access to portfolios 1-8 (or 50% Equity) while an Aggressive
            investor has access to all 16 portfolios (up to 90% Equity).
            On a default basis, GOE is configured for three Risk Profile levels, but
            it can be customized for up to five levels.
        infusion_type (Infusiontype5): Indicates the frequency of cash flows –
            Determines the cash flow array corresponding to the ‘infusions’ parameter.
        curr_date (str): This is an optional parameter that can be used to simulate
            the current date being the specified value. <br>
            GOE will process the request as if you are making the API call on the
            specified date. If not passed, the current system date will be used as
            the current date.
        goal_profile_list (List[GoalProfile]): A list containing parameters for
            different goals. Each goal in the list would have a goal ID,
            goal amount, start date, end date, and priority.For subsequent calls,
            the goalProfileList needs to be updated based on the time that has
            elapsed. If a goal has expired, then it needs to be removed from the
            goalProfileList.
        plan_id (str): Plan ID of the participant.
        participant_id (str): ID of the participant.
        source_id (str): Source ID of the plan
        reallocate (bool): If the client wants GOE to reallocate between the
            scheduled re-allocation dates, this should be set to ‘true’.
        calibrate_goal_rec (bool): If the client wants GOE to reallocate between the
            scheduled re-allocation dates, this should be set to ‘true’.
            Setting this parameter to true will ensure that GOE runs a calibration
            logic for the recommendations so                 that incorporating the
            recommendations would result in reaching the goal probability target (for
            example, 85% for a Need priority).
        assets_held_away (List[FixedAsset]): Array of AHA objects- Only available
            through certain modes on Admin Portal.
        wealth_path_probabilities (List[float]): This is an optional input parameter.
            If this input is passed, instead of one wealth path, <br>         the GOE
            output will have 3 wealth paths –one path at the goal priority
            probability level and <br>             one path for each of the 2
            probability values passed in this input array.
        fixed_assets (List[AssetsHeldAwayInfo]): Array of fixed asset objects- Only
            available through certain modes on Admin Portal.
        policy (List[PolicyDetails]): Contains details about the policy validation
            associated with the Platform Partner. In this example,             the
            array includes participantType and callType, however, the dictionary is
            open and Platform Partners are able to send other fields to define their
            respective API Policy.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "is_new_goal_priority": "isNewGoalPriority",
        "is_new_risk_profile": "isNewRiskProfile",
        "is_new_investment_tenure": "isNewInvestmentTenure",
        "is_new_goal": "isNewGoal",
        "get_path": "getPath",
        "reallocation_freq": "reallocationFreq",
        "initial_investment": "initialInvestment",
        "current_wealth": "currentWealth",
        "current_portfolio_id": "currentPortfolioId",
        "infusions": "infusions",
        "risk_profile": "riskProfile",
        "infusion_type": "infusionType",
        "goal_profile_list": "goalProfileList",
        "cashflow_date": "cashflowDate",
        "curr_date": "currDate",
        "plan_id": "planID",
        "participant_id": "participantID",
        "source_id": "sourceID",
        "reallocate": "reallocate",
        "calibrate_goal_rec": "calibrateGoalRec",
        "assets_held_away": "assetsHeldAway",
        "wealth_path_probabilities": "wealthPathProbabilities",
        "fixed_assets": "fixedAssets",
        "policy": "policy",
    }

    _optionals = [
        "cashflow_date",
        "curr_date",
        "plan_id",
        "participant_id",
        "source_id",
        "reallocate",
        "calibrate_goal_rec",
        "assets_held_away",
        "wealth_path_probabilities",
        "fixed_assets",
        "policy",
    ]

    _nullables = [
        "cashflow_date",
        "current_wealth",
        "current_portfolio_id",
        "plan_id",
        "participant_id",
        "source_id",
    ]

    def __init__(
        self,
        is_new_goal_priority=None,
        is_new_risk_profile=None,
        is_new_investment_tenure=None,
        is_new_goal=None,
        get_path=None,
        reallocation_freq=None,
        initial_investment=None,
        current_wealth=None,
        current_portfolio_id=None,
        infusions=None,
        risk_profile=None,
        infusion_type=None,
        goal_profile_list=None,
        cashflow_date=APIHelper.SKIP,
        curr_date="System Date",
        plan_id=APIHelper.SKIP,
        participant_id=APIHelper.SKIP,
        source_id=APIHelper.SKIP,
        reallocate=False,
        calibrate_goal_rec=False,
        assets_held_away=APIHelper.SKIP,
        wealth_path_probabilities=APIHelper.SKIP,
        fixed_assets=APIHelper.SKIP,
        policy=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a UnifiedPortfolioAdviceInputModelV4 instance."""
        # Initialize members of the class
        self.is_new_goal_priority = is_new_goal_priority
        self.is_new_risk_profile = is_new_risk_profile
        self.is_new_investment_tenure = is_new_investment_tenure
        self.is_new_goal = is_new_goal
        if cashflow_date is not APIHelper.SKIP:
            self.cashflow_date = cashflow_date
        self.get_path = get_path
        self.reallocation_freq = reallocation_freq
        self.initial_investment = initial_investment
        self.current_wealth = current_wealth
        self.current_portfolio_id = current_portfolio_id
        self.infusions = infusions
        self.risk_profile = risk_profile
        self.infusion_type = infusion_type
        self.curr_date = curr_date
        self.goal_profile_list = goal_profile_list
        if plan_id is not APIHelper.SKIP:
            self.plan_id = plan_id
        if participant_id is not APIHelper.SKIP:
            self.participant_id = participant_id
        if source_id is not APIHelper.SKIP:
            self.source_id = source_id
        self.reallocate = reallocate
        self.calibrate_goal_rec = calibrate_goal_rec
        if assets_held_away is not APIHelper.SKIP:
            self.assets_held_away = assets_held_away
        if wealth_path_probabilities is not APIHelper.SKIP:
            self.wealth_path_probabilities = wealth_path_probabilities
        if fixed_assets is not APIHelper.SKIP:
            self.fixed_assets = fixed_assets
        if policy is not APIHelper.SKIP:
            self.policy = policy

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        is_new_goal_priority =\
            dictionary.get("isNewGoalPriority")\
            if "isNewGoalPriority" in dictionary.keys()\
                else None
        is_new_risk_profile =\
            dictionary.get("isNewRiskProfile")\
            if "isNewRiskProfile" in dictionary.keys()\
                else None
        is_new_investment_tenure =\
            dictionary.get("isNewInvestmentTenure")\
            if "isNewInvestmentTenure" in dictionary.keys()\
                else None
        is_new_goal =\
            dictionary.get("isNewGoal")\
            if "isNewGoal" in dictionary.keys()\
                else None
        get_path =\
            dictionary.get("getPath")\
            if "getPath" in dictionary.keys()\
                else None
        reallocation_freq =\
            dictionary.get("reallocationFreq")\
            if dictionary.get("reallocationFreq")\
                else None
        initial_investment =\
            dictionary.get("initialInvestment")\
            if dictionary.get("initialInvestment")\
                else None
        current_wealth =\
            dictionary.get("currentWealth")\
            if dictionary.get("currentWealth")\
                else None
        current_portfolio_id =\
            dictionary.get("currentPortfolioId")\
            if dictionary.get("currentPortfolioId")\
                else None
        infusions =\
            dictionary.get("infusions")\
            if dictionary.get("infusions")\
                else None
        risk_profile =\
            dictionary.get("riskProfile")\
            if dictionary.get("riskProfile")\
                else None
        infusion_type =\
            dictionary.get("infusionType")\
            if dictionary.get("infusionType")\
                else None
        goal_profile_list = None
        if dictionary.get("goalProfileList") is not None:
            goal_profile_list = [
                GoalProfile.from_dictionary(x)
                    for x in dictionary.get("goalProfileList")
            ]
        cashflow_date =\
            dictionary.get("cashflowDate")\
            if "cashflowDate" in dictionary.keys()\
                else APIHelper.SKIP
        curr_date =\
            dictionary.get("currDate")\
            if dictionary.get("currDate")\
                else "System Date"
        plan_id =\
            dictionary.get("planID")\
            if "planID" in dictionary.keys()\
                else APIHelper.SKIP
        participant_id =\
            dictionary.get("participantID")\
            if "participantID" in dictionary.keys()\
                else APIHelper.SKIP
        source_id =\
            dictionary.get("sourceID")\
            if "sourceID" in dictionary.keys()\
                else APIHelper.SKIP
        reallocate =\
            dictionary.get("reallocate")\
            if dictionary.get("reallocate")\
                else False
        calibrate_goal_rec =\
            dictionary.get("calibrateGoalRec")\
            if dictionary.get("calibrateGoalRec")\
                else False
        assets_held_away = None
        if dictionary.get("assetsHeldAway") is not None:
            assets_held_away = [
                FixedAsset.from_dictionary(x)
                    for x in dictionary.get("assetsHeldAway")
            ]
        else:
            assets_held_away = APIHelper.SKIP
        wealth_path_probabilities =\
            dictionary.get("wealthPathProbabilities")\
            if dictionary.get("wealthPathProbabilities")\
                else APIHelper.SKIP
        fixed_assets = None
        if dictionary.get("fixedAssets") is not None:
            fixed_assets = [
                AssetsHeldAwayInfo.from_dictionary(x)
                    for x in dictionary.get("fixedAssets")
            ]
        else:
            fixed_assets = APIHelper.SKIP
        policy = None
        if dictionary.get("policy") is not None:
            policy = [
                PolicyDetails.from_dictionary(x)
                    for x in dictionary.get("policy")
            ]
        else:
            policy = APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(is_new_goal_priority,
                   is_new_risk_profile,
                   is_new_investment_tenure,
                   is_new_goal,
                   get_path,
                   reallocation_freq,
                   initial_investment,
                   current_wealth,
                   current_portfolio_id,
                   infusions,
                   risk_profile,
                   infusion_type,
                   goal_profile_list,
                   cashflow_date,
                   curr_date,
                   plan_id,
                   participant_id,
                   source_id,
                   reallocate,
                   calibrate_goal_rec,
                   assets_held_away,
                   wealth_path_probabilities,
                   fixed_assets,
                   policy,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _is_new_goal_priority=self.is_new_goal_priority
        _is_new_risk_profile=self.is_new_risk_profile
        _is_new_investment_tenure=self.is_new_investment_tenure
        _is_new_goal=self.is_new_goal
        _cashflow_date=(
            self.cashflow_date
            if hasattr(self, "cashflow_date")
            else None
        )
        _get_path=self.get_path
        _reallocation_freq=self.reallocation_freq
        _initial_investment=self.initial_investment
        _current_wealth=self.current_wealth
        _current_portfolio_id=self.current_portfolio_id
        _infusions=self.infusions
        _risk_profile=self.risk_profile
        _infusion_type=self.infusion_type
        _curr_date=(
            self.curr_date
            if hasattr(self, "curr_date")
            else None
        )
        _goal_profile_list=self.goal_profile_list
        _plan_id=(
            self.plan_id
            if hasattr(self, "plan_id")
            else None
        )
        _participant_id=(
            self.participant_id
            if hasattr(self, "participant_id")
            else None
        )
        _source_id=(
            self.source_id
            if hasattr(self, "source_id")
            else None
        )
        _reallocate=(
            self.reallocate
            if hasattr(self, "reallocate")
            else None
        )
        _calibrate_goal_rec=(
            self.calibrate_goal_rec
            if hasattr(self, "calibrate_goal_rec")
            else None
        )
        _assets_held_away=(
            self.assets_held_away
            if hasattr(self, "assets_held_away")
            else None
        )
        _wealth_path_probabilities=(
            self.wealth_path_probabilities
            if hasattr(self, "wealth_path_probabilities")
            else None
        )
        _fixed_assets=(
            self.fixed_assets
            if hasattr(self, "fixed_assets")
            else None
        )
        _policy=(
            self.policy
            if hasattr(self, "policy")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"is_new_goal_priority={_is_new_goal_priority!r}, "
            f"is_new_risk_profile={_is_new_risk_profile!r}, "
            f"is_new_investment_tenure={_is_new_investment_tenure!r}, "
            f"is_new_goal={_is_new_goal!r}, "
            f"cashflow_date={_cashflow_date!r}, "
            f"get_path={_get_path!r}, "
            f"reallocation_freq={_reallocation_freq!r}, "
            f"initial_investment={_initial_investment!r}, "
            f"current_wealth={_current_wealth!r}, "
            f"current_portfolio_id={_current_portfolio_id!r}, "
            f"infusions={_infusions!r}, "
            f"risk_profile={_risk_profile!r}, "
            f"infusion_type={_infusion_type!r}, "
            f"curr_date={_curr_date!r}, "
            f"goal_profile_list={_goal_profile_list!r}, "
            f"plan_id={_plan_id!r}, "
            f"participant_id={_participant_id!r}, "
            f"source_id={_source_id!r}, "
            f"reallocate={_reallocate!r}, "
            f"calibrate_goal_rec={_calibrate_goal_rec!r}, "
            f"assets_held_away={_assets_held_away!r}, "
            f"wealth_path_probabilities={_wealth_path_probabilities!r}, "
            f"fixed_assets={_fixed_assets!r}, "
            f"policy={_policy!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _is_new_goal_priority=self.is_new_goal_priority
        _is_new_risk_profile=self.is_new_risk_profile
        _is_new_investment_tenure=self.is_new_investment_tenure
        _is_new_goal=self.is_new_goal
        _cashflow_date=(
            self.cashflow_date
            if hasattr(self, "cashflow_date")
            else None
        )
        _get_path=self.get_path
        _reallocation_freq=self.reallocation_freq
        _initial_investment=self.initial_investment
        _current_wealth=self.current_wealth
        _current_portfolio_id=self.current_portfolio_id
        _infusions=self.infusions
        _risk_profile=self.risk_profile
        _infusion_type=self.infusion_type
        _curr_date=(
            self.curr_date
            if hasattr(self, "curr_date")
            else None
        )
        _goal_profile_list=self.goal_profile_list
        _plan_id=(
            self.plan_id
            if hasattr(self, "plan_id")
            else None
        )
        _participant_id=(
            self.participant_id
            if hasattr(self, "participant_id")
            else None
        )
        _source_id=(
            self.source_id
            if hasattr(self, "source_id")
            else None
        )
        _reallocate=(
            self.reallocate
            if hasattr(self, "reallocate")
            else None
        )
        _calibrate_goal_rec=(
            self.calibrate_goal_rec
            if hasattr(self, "calibrate_goal_rec")
            else None
        )
        _assets_held_away=(
            self.assets_held_away
            if hasattr(self, "assets_held_away")
            else None
        )
        _wealth_path_probabilities=(
            self.wealth_path_probabilities
            if hasattr(self, "wealth_path_probabilities")
            else None
        )
        _fixed_assets=(
            self.fixed_assets
            if hasattr(self, "fixed_assets")
            else None
        )
        _policy=(
            self.policy
            if hasattr(self, "policy")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"is_new_goal_priority={_is_new_goal_priority!s}, "
            f"is_new_risk_profile={_is_new_risk_profile!s}, "
            f"is_new_investment_tenure={_is_new_investment_tenure!s}, "
            f"is_new_goal={_is_new_goal!s}, "
            f"cashflow_date={_cashflow_date!s}, "
            f"get_path={_get_path!s}, "
            f"reallocation_freq={_reallocation_freq!s}, "
            f"initial_investment={_initial_investment!s}, "
            f"current_wealth={_current_wealth!s}, "
            f"current_portfolio_id={_current_portfolio_id!s}, "
            f"infusions={_infusions!s}, "
            f"risk_profile={_risk_profile!s}, "
            f"infusion_type={_infusion_type!s}, "
            f"curr_date={_curr_date!s}, "
            f"goal_profile_list={_goal_profile_list!s}, "
            f"plan_id={_plan_id!s}, "
            f"participant_id={_participant_id!s}, "
            f"source_id={_source_id!s}, "
            f"reallocate={_reallocate!s}, "
            f"calibrate_goal_rec={_calibrate_goal_rec!s}, "
            f"assets_held_away={_assets_held_away!s}, "
            f"wealth_path_probabilities={_wealth_path_probabilities!s}, "
            f"fixed_assets={_fixed_assets!s}, "
            f"policy={_policy!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
